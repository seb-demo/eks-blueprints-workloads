apiVersion: karpenter.sh/v1alpha5
kind: Provisioner
metadata:
  name: burnham
spec:
  requirements:
    # - key: 'node.kubernetes.io/instance-type'
    #   operator: In
    #   values: ['m5.xlarge', 'm5a.xlarge', 'm5ad.xlarge', 'm5d.xlarge', 't2.xlarge', 't3.xlarge', 't3a.xlarge']
    - key: 'kubernetes.io/arch'
      operator: In
      values: ['amd64']
    - key: karpenter.sh/capacity-type
      operator: In
      values: ['on-demand', 'spot']
  provider:
    apiVersion: extensions.karpenter.sh/v1alpha1
    kind: AWS
    instanceProfile: {{ .Values.spec.karpenterInstanceProfile }}
    securityGroupSelector:
      kubernetes.io/cluster/blueprints-dev-blueprint: '*'
      #aws:eks:cluster-name: 'blueprints-dev-blueprint'
    subnetSelector:
      kubernetes.io/cluster/blueprints-dev-blueprint: '*'
      #aws:cloudformation:stack-name: 'blueprints-dev-blueprints-dev-blueprint'
    tags:
      karpenter.sh/cluster_name: blueprints-{{ .Values.spec.env }}-blueprint
      karpenter.sh/provisioner: burnham
  ttlSecondsAfterEmpty: 0
  limits:
    resources:
      cpu: '200'
  labels:
    billing-team: burnham
    team: burnham
    type: karpenter
  taints:
    - key: burnham
      value: 'true'
      effect: NoSchedule
